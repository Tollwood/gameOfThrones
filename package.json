{
  "name": "got",
  "version": "1.0.0",
  "description": "Web Server to enable multiplayer for scotland united",
  "private": true,
  "scripts": {
    "cleanup-build-dir": "rm -rf build",
    "build-client": "npm --prefix client run build --prod",
    "copy-client": "gulp copy_client",
    "build-server": "npm --prefix server run build",
    "copy-server": "gulp copy_server",
    "full-build-prod": "npm run cleanup-build-dir && npm --prefix client install && npm run build-client",
    "start": "node dist/index.js"
  },
  "author": "Tobias Manger",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.1"
  }
}
